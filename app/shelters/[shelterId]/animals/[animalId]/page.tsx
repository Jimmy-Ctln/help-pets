'use client'

import React, { useEffect } from 'react'

export default function AnimalProfile({ params }: { params: Promise<{ id: string }> }) {
  // const { id } = React.use(params)
  // console.log(id)
}
